<div class="op-cont">
    <h1>Eat Da Burger!</h1>

    <h2>Burgers on the menu!</h2>

    <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devour=true}}
        {{/unless}}
        {{/each}}
    </ul>

    <h2>Burgers you demolished!</h2>

    <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block devour=false}}
        {{/if}}
        {{/each}}
    </ul>

    <h2>Add a burger, it's okay! This doesn't cost real money!</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="ca">What burger is next?</label>
            <input type="text" id="ca" name="name">
        </div>

        <div class="form-group">
            <input type="radio" name="devoured" value="0" checked> Buy it.
        </div>

        <button type="submit">Hulk Smash This Button!</button>
    </form>
</div>